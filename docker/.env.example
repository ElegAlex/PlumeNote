# ===========================================
# PlumeNote - Configuration Docker
# ===========================================

# Environnement (development | production)
# IMPORTANT: En production avec HTTPS, utiliser "production"
# En developpement ou sans HTTPS valide, utiliser "development"
NODE_ENV=production

# PostgreSQL
POSTGRES_USER=plumenote
POSTGRES_PASSWORD=CHANGEZ_MOI_mot_de_passe_securise
POSTGRES_DB=plumenote

# Redis
REDIS_PASSWORD=CHANGEZ_MOI_redis_password

# JWT & Cookies (generer avec: openssl rand -base64 32)
JWT_SECRET=CHANGEZ_MOI_jwt_secret_32_chars_minimum
COOKIE_SECRET=CHANGEZ_MOI_cookie_secret_32_chars

# CORS - Domaines autorises (virgule separes)
CORS_ORIGINS=https://votredomaine.fr,https://www.votredomaine.fr
